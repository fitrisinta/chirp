<div class="main users-index">
  <div class="container">
    <h1 class="users-heading">All Users</h1>
    <!-- Add an each method inside <% %> -->
    <% @users.each do |user| %>
      <div class="users-index-item">
        <div class="user-right">
          <!-- Make a link to the "User details" page -->
          <%= link_to(user.name, "/users/#{user.id}") %>
        </div>
      </div>
    <% end %>
  </div>
</div>